## Implementation Plan

### Tokenizer

- Update the tokenizer implementation to use english focused dataset
  - Same dataset needs to be used for training LLM also so validate if new tokenizer can be used for default dataset also
  - Target 5+ compression rate otherwise go with default tokenizer
